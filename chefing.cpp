#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t, n;
    string s;
    int a[26] = {0};

    cin >> t;
    while (t--)
    {
        cin >> n;
        while (n--)
        {
            cin >> s;

            for (int i = 0; i < s.size(); i++)
            {
            }
        }
    }
}